---
phase: 01-electron-shell-and-build-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/desktop/package.json
  - apps/desktop/electron.vite.config.ts
  - apps/desktop/tsconfig.json
  - apps/desktop/tsconfig.node.json
  - apps/desktop/tsconfig.web.json
  - apps/desktop/src/main/index.ts
  - apps/desktop/src/main/ipc-handlers.ts
  - apps/desktop/src/main/protocol.ts
  - apps/desktop/src/preload/index.ts
  - apps/desktop/src/renderer/index.html
  - apps/desktop/src/renderer/src/main.tsx
  - apps/desktop/src/renderer/src/App.tsx
  - apps/desktop/src/renderer/src/app.css
autonomous: false
requirements:
  - INFRA-05
  - INFRA-06
  - INFRA-07

must_haves:
  truths:
    - "pnpm dev launches an Electron window displaying a React page"
    - "Hot module reload updates the renderer without full page refresh"
    - "Renderer cannot access Node.js APIs directly -- window.require is undefined"
    - "window.api exposes typed IPC functions (selectFolder, scanFolder, trashFiles)"
    - "app:// protocol resolves local file paths securely"
  artifacts:
    - path: "apps/desktop/src/main/index.ts"
      provides: "Electron main process entry -- window creation, menu, app lifecycle"
      min_lines: 40
    - path: "apps/desktop/src/main/protocol.ts"
      provides: "Custom app:// protocol handler"
      exports: ["registerSchemes", "registerProtocolHandlers"]
    - path: "apps/desktop/src/main/ipc-handlers.ts"
      provides: "ipcMain.handle registrations for all IPC channels"
      exports: ["registerIpcHandlers"]
    - path: "apps/desktop/src/preload/index.ts"
      provides: "contextBridge.exposeInMainWorld with typed ElectronAPI"
      min_lines: 10
    - path: "apps/desktop/electron.vite.config.ts"
      provides: "electron-vite config for main/preload/renderer"
    - path: "apps/desktop/src/renderer/src/App.tsx"
      provides: "Root React component"
  key_links:
    - from: "apps/desktop/src/preload/index.ts"
      to: "packages/types/src/ipc.ts"
      via: "imports IPC_CHANNELS and ElectronAPI type"
      pattern: "import.*@photo-culler/types"
    - from: "apps/desktop/src/main/ipc-handlers.ts"
      to: "packages/types/src/ipc.ts"
      via: "imports IPC_CHANNELS for handler registration"
      pattern: "IPC_CHANNELS"
    - from: "apps/desktop/src/main/index.ts"
      to: "apps/desktop/src/main/protocol.ts"
      via: "calls registerSchemes before app.whenReady, registerProtocolHandlers after"
      pattern: "registerSchemes|registerProtocolHandlers"
    - from: "apps/desktop/src/renderer/src/App.tsx"
      to: "window.api"
      via: "accesses typed IPC through global window.api"
      pattern: "window\\.api"
---

<objective>
Create the Electron desktop app with electron-vite, typed IPC via contextBridge, custom app:// protocol, and a React renderer with HMR.

Purpose: This is the actual desktop application shell. It wires the main process, preload bridge, and renderer together with proper security (contextIsolation, sandbox, no nodeIntegration). The typed IPC contracts from @photo-culler/types are implemented here.
Output: A working Electron app that launches via `pnpm dev` with HMR, displays a React page, and has typed IPC + secure protocol ready for feature work.
</objective>

<execution_context>
@/Users/martinhess/workspace/photo-culler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/martinhess/workspace/photo-culler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-electron-shell-and-build-pipeline/01-RESEARCH.md
@.planning/phases/01-electron-shell-and-build-pipeline/01-CONTEXT.md
@.planning/phases/01-electron-shell-and-build-pipeline/01-01-SUMMARY.md

<interfaces>
<!-- From @photo-culler/types (created in Plan 01) -->

From packages/types/src/ipc.ts:
```typescript
export const IPC_CHANNELS = {
  SELECT_FOLDER: 'dialog:select-folder',
  SCAN_FOLDER: 'fs:scan-folder',
  TRASH_FILES: 'fs:trash-files',
} as const;

export interface ElectronAPI {
  selectFolder: () => Promise<string | null>;
  scanFolder: (folderPath: string) => Promise<ImageFileInfo[]>;
  trashFiles: (filePaths: string[]) => Promise<TrashResult>;
}
```

From packages/types/src/image.ts:
```typescript
export interface ImageFileInfo {
  path: string;
  name: string;
  extension: string;
  size: number;
  lastModified: number;
  width?: number;
  height?: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Electron app with electron-vite, IPC, and protocol</name>
  <files>
    apps/desktop/package.json,
    apps/desktop/electron.vite.config.ts,
    apps/desktop/tsconfig.json,
    apps/desktop/tsconfig.node.json,
    apps/desktop/tsconfig.web.json,
    apps/desktop/src/main/index.ts,
    apps/desktop/src/main/ipc-handlers.ts,
    apps/desktop/src/main/protocol.ts,
    apps/desktop/src/preload/index.ts,
    apps/desktop/src/renderer/index.html,
    apps/desktop/src/renderer/src/main.tsx,
    apps/desktop/src/renderer/src/App.tsx,
    apps/desktop/src/renderer/src/app.css
  </files>
  <action>
    Install dependencies in apps/desktop:
    ```
    pnpm add --filter @photo-culler/desktop electron react react-dom
    pnpm add -D --filter @photo-culler/desktop electron-vite electron-builder @vitejs/plugin-react typescript @types/react @types/react-dom tailwindcss vitest @photo-culler/types@workspace:*
    ```

    **apps/desktop/package.json:**
    - name: "@photo-culler/desktop"
    - version: "0.1.0"
    - main: "./out/main/index.js" (electron-vite output)
    - scripts: dev ("electron-vite dev"), build ("electron-vite build"), preview ("electron-vite preview"), format/format:check/lint (prettier + eslint on src/)
    - Move electron to devDependencies (electron-builder bundles it)

    **apps/desktop/electron.vite.config.ts:**
    - Per research Pattern 1. Three build targets: main, preload, renderer.
    - main + preload: externalizeDepsPlugin(), resolve.alias for @photo-culler/types pointing to ../../packages/types/src
    - renderer: @vitejs/plugin-react(), resolve.alias for @renderer -> src/renderer/src, @photo-culler/types -> ../../packages/types/src

    **tsconfig files:**
    - tsconfig.json: references to tsconfig.node.json and tsconfig.web.json
    - tsconfig.node.json: extends @photo-culler/tsconfig/node.json, include src/main and src/preload
    - tsconfig.web.json: extends @photo-culler/tsconfig/react.json, include src/renderer

    **apps/desktop/src/main/protocol.ts:**
    - Per research Pattern 3. registerSchemes() calls protocol.registerSchemesAsPrivileged before app.whenReady(). registerProtocolHandlers() calls protocol.handle('app', ...) after app.whenReady().
    - Security: normalize paths with path.normalize, use net.fetch for file serving.

    **apps/desktop/src/main/ipc-handlers.ts:**
    - Import IPC_CHANNELS from @photo-culler/types.
    - registerIpcHandlers() function that sets up ipcMain.handle for each channel.
    - SELECT_FOLDER: opens dialog.showOpenDialog with properties ['openDirectory'], returns selected path or null.
    - SCAN_FOLDER: placeholder -- returns empty array for now (implemented in Phase 2).
    - TRASH_FILES: placeholder -- returns { success: true, trashedCount: 0 } for now (implemented in Phase 3).

    **apps/desktop/src/main/index.ts:**
    - Per research Pattern 5. Import registerSchemes, call it BEFORE app.whenReady().
    - In app.whenReady(): call registerProtocolHandlers(), registerIpcHandlers().
    - Create BrowserWindow: minWidth 800, minHeight 600, show false, webPreferences.preload points to ../preload/index.js.
    - mainWindow.maximize(), show on ready-to-show.
    - Load renderer: ELECTRON_RENDERER_URL in dev, loadFile in prod.
    - Standard OS menu bar with File, Edit, View, Window, Help structure using Menu.buildFromTemplate.
    - Handle app.on('window-all-closed') -- quit on non-macOS, on macOS do nothing (standard behavior).
    - Handle app.on('activate') -- recreate window on macOS if no windows exist.

    **apps/desktop/src/preload/index.ts:**
    - Per research Pattern 2. Import contextBridge, ipcRenderer from electron. Import IPC_CHANNELS from @photo-culler/types.
    - Create api object implementing ElectronAPI interface using ipcRenderer.invoke.
    - contextBridge.exposeInMainWorld('api', api).
    - NEVER expose raw ipcRenderer. Only expose individual typed functions.

    **apps/desktop/src/renderer/index.html:**
    - Standard HTML5 boilerplate with div#root and script tag pointing to src/main.tsx.

    **apps/desktop/src/renderer/src/app.css:**
    - Single line: @import "tailwindcss";
    - Tailwind CSS v4 zero-config -- no tailwind.config.js needed.

    **apps/desktop/src/renderer/src/main.tsx:**
    - Import React, createRoot from react-dom/client, App component, app.css.
    - Render App into #root.

    **apps/desktop/src/renderer/src/App.tsx:**
    - Simple component showing app name "Photo Culler" and a button that calls window.api.selectFolder() and displays the result.
    - This proves the IPC bridge works end-to-end.

    After all files created, run `pnpm install` to link workspace dependencies, then `pnpm build` to verify compilation.
  </action>
  <verify>
    <automated>cd /Users/martinhess/workspace/photo-culler && pnpm install && pnpm build</automated>
  </verify>
  <done>All files created, pnpm build succeeds (electron-vite compiles main/preload/renderer to out/), typed IPC channels match @photo-culler/types contracts</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Electron app launches with HMR, IPC, and security</name>
  <action>
    Human verifies the Electron app works correctly by running pnpm dev and testing IPC bridge, security isolation, HMR, and window configuration.
  </action>
  <what-built>Electron desktop app with React renderer, typed IPC via contextBridge, custom app:// protocol, and hot module reload via electron-vite.</what-built>
  <how-to-verify>
    1. Run `cd /Users/martinhess/workspace/photo-culler && pnpm dev`
    2. Verify: Electron window opens maximized showing a React page with "Photo Culler" heading
    3. Verify: Window has standard OS menu bar (File, Edit, View, Window, Help)
    4. Verify: Click the "Select Folder" button -- native folder dialog opens
    5. Verify: Open DevTools (Cmd+Option+I) and type `window.require` -- should be undefined (proving nodeIntegration is off)
    6. Verify: In DevTools console, type `window.api` -- should show object with selectFolder, scanFolder, trashFiles functions
    7. Verify HMR: Edit apps/desktop/src/renderer/src/App.tsx (change the heading text) -- the change should appear without a full page reload
    8. Close the app (Cmd+Q)
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds -- electron-vite compiles all three targets
2. `pnpm dev` launches Electron window with React content
3. IPC bridge works: window.api exists with typed functions
4. Protocol module exists with correct timing (registerSchemes before ready)
5. `pnpm format:check && pnpm lint` still pass with new code
</verification>

<success_criteria>
- Electron window launches maximized with React content via `pnpm dev`
- HMR works in renderer (edit component, see change without reload)
- window.api exposes selectFolder/scanFolder/trashFiles (typed IPC)
- window.require is undefined (nodeIntegration off, contextIsolation on)
- app:// protocol module exists with registerSchemes/registerProtocolHandlers
- Standard OS menu bar present
- Minimum window size 800x600 enforced
</success_criteria>

<output>
After completion, create `.planning/phases/01-electron-shell-and-build-pipeline/01-02-SUMMARY.md`
</output>
